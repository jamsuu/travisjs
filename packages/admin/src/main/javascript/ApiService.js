export default class ApiService {

    get(url, options) {
        return Promise.resolve({});
    }

    post(url, options) {
        return Promise.resolve({});
    }

    put(url, options) {
        return Promise.resolve({});
    }

    delete(url, options) {
        return Promise.resolve({});
    }
}